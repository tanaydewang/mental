# Environment variables — copy to `.env` and fill in
# Supabase credentials are pre-provisioned in this Bolt environment.
VITE_SUPABASE_URL=your_supabase_project_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key
